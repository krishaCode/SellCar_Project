spring.application.name=Sell_Car

spring.datasource.url=jdbc:mysql://localhost:3306/sellcar_db
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT signing key (Base64 encoded). Must decode to at least 32 bytes (256 bits).
# Generate a secure key with (Java):
# System.out.println(java.util.Base64.getEncoder().encodeToString(io.jsonwebtoken.security.Keys.secretKeyFor(io.jsonwebtoken.SignatureAlgorithm.HS256).getEncoded()));
# Or using OpenSSL (Git Bash / WSL): openssl rand -base64 32
# Example placeholder (32 bytes when Base64-decoded):
jwt.secret=AAECAwQFBgcICQoLDA0ODxAREhMUFRYXGBkaGxwdHh8=
